<block>
    <view class="waterfall-box">
        <view class="tit">
            <image src="{{tits}}" mode="widthFix" class="pic"></image>
        </view>
        <view class="waterfall">
            <view class="List-left">
                <view class="startsCard" wx:key="index" wx:for="{{leftData}}" wx:for-item="item" wx:for-index="index">
                    <block wx:if="{{android}}">
                        <view class="andro" bindtap="toLink" data-e-tap-so="this" data-e-tap-a-a="{{item}}"></view>
                    </block>
                    <view class="box">
                        <image src="{{item.card_image_path}}" mode="widthFix" class="pic" bindtap="toLink" data-e-tap-so="this" data-e-tap-a-a="{{item}}"></image>
                        <view class="seaTit">{{item.title}}</view>
                        <view class="main">
                            <image src="{{item.avatar_image_path}}" mode="widthFix" class="headPic"></image><text class="tits">{{item.nickname}}</text>
                            <view bindtap="dianzna" class="star" data-e-tap-so="this" data-e-tap-a-a="{{item.id}}"></view>
                            <image src="{{!item.is_like ? heart : hearts}}" mode="widthFix" class="heart"></image><text class="nums">{{item.like}}</text>
                        </view>
                    </view>
                </view>
            </view>
            <view class="List-right">
                <view class="startsCard" wx:key="index" wx:for="{{rightData}}" wx:for-item="item" wx:for-index="index">
                    <block wx:if="{{android}}">
                        <view class="andro" bindtap="toLink" data-e-tap-so="this" data-e-tap-a-a="{{item}}"></view>
                    </block>
                    <view class="box">
                        <image src="{{item.card_image_path}}" mode="widthFix" class="pic" bindtap="toLink" data-e-tap-so="this" data-e-tap-a-a="{{item}}"></image>
                        <view class="seaTit">{{item.title}}</view>
                        <view class="main">
                            <view class="headPic">
                                <image src="{{item.avatar_image_path}}" mode="widthFix"></image>
                            </view><text class="tits">{{item.nickname}}</text>
                            <view bindtap="dianzna" class="star" data-e-tap-so="this" data-e-tap-a-a="{{item.id}}"></view>
                            <image src="{{!item.is_like ? heart : hearts}}" mode="widthFix" class="heart"></image><text class="nums">{{item.like}}</text>
                        </view>
                    </view>
                </view>
            </view>
        </view>
    </view>
</block>