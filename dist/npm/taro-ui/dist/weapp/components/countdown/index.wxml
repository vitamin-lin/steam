<block>
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <block wx:if="{{isShowDay}}">
            <at-countdown-item num="{{_day}}" __triggerObserer="{{ _triggerObserer }}" separator="{{format.day}}"></at-countdown-item>
        </block>
        <at-countdown-item num="{{_hours}}" __triggerObserer="{{ _triggerObserer }}" separator="{{format.hours}}"></at-countdown-item>
        <at-countdown-item num="{{_minutes}}" __triggerObserer="{{ _triggerObserer }}" separator="{{format.minutes}}"></at-countdown-item>
        <at-countdown-item num="{{_seconds}}" __triggerObserer="{{ _triggerObserer }}" separator="{{format.seconds}}"></at-countdown-item>
    </view>
</block>