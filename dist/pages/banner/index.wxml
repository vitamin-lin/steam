<block>
    <block wx:if="{{!banner || banner.length <= 0}}">
        <view></view>
    </block>
    <view class="page-wrap" wx:else>
        <view class="banner-wrap">
            <view wx:key="$original.id" class="banner-cn" catchtap="anonymousFunc0" wx:for="{{loopArray0}}" wx:for-item="b" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{__index0}}">
                <image src="{{b.$original.banner_image_url}}" class="banner-pic" mode="widthFix"></image>
            </view>
        </view>
    </view>
</block>