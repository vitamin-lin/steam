<block>
    <view class="user">
        <view class="user_box">
            <user-header __triggerObserer="{{ _triggerObserer }}"></user-header>
            <view class="getUser">
                <logins __triggerObserer="{{ _triggerObserer }}"></logins>
            </view>
            <view class="panels" bindtap="links"></view>
            <view class="main">
                <at-list className="panel_top address" __triggerObserer="{{ _triggerObserer }}" hasBorder="{{false}}">
                    <at-list-item title="我的记录" __triggerObserer="{{ _triggerObserer }}" arrow="right" hasBorder="{{false}}" thumb="{{biaoqian}}" bindonclick="getAddress" iconInfo="{{anonymousState__temp}}" __fn_onClick="{{true}}"></at-list-item>
                    <at-list-item title="我的成就" __triggerObserer="{{ _triggerObserer }}" arrow="right" hasBorder="{{false}}" thumb="{{quan}}" bindonclick="getCoupons" className="quan" iconInfo="{{anonymousState__temp2}}" __fn_onClick="{{true}}"></at-list-item>
                    <at-list-item title="客服" __triggerObserer="{{ _triggerObserer }}" arrow="right" hasBorder="{{false}}" thumb="{{icons}}" className="icons" iconInfo="{{anonymousState__temp3}}"></at-list-item>
                </at-list>
            </view>
        </view>
    </view>
</block>